; Override PHP-FPM pool settings (loaded after www.conf, last value wins).
; Prevents one stuck request from blocking the whole site.
[www]
pm.max_children = 10
pm.start_servers = 2
pm.min_spare_servers = 1
pm.max_spare_servers = 5
request_terminate_timeout = 60
